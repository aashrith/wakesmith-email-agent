# Copy to .env and fill in. Never commit the real .env.

# Your OpenRouter key — https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Password for the GreenMail test mailboxes (any value works; GreenMail
# accepts whatever you configure it with in docker-compose.yml).
EMAIL_PASSWORD=agent-pass
